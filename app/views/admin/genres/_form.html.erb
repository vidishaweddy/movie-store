<%= simple_form_for [:admin,@genre], html: { class: 'pure-form pure-form-aligned' } do |f| %>
  <div class="pure-g">
    <div class="pure-u-1 pure-u-md-1-1">
      <%= f.input :name, input_html: { class: 'pure-input-1'} %>
    </div>
  </div>

  <div class="pure-controls">
    <%= save_button %>
    <%= cancel_button(admin_genres_path) %>
  </div>
<% end %>
